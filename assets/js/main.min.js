fetch('https://qa-backend.jitfin.co/api/anchor/find?gstin=33AACCG0527D1ZB',{
    method: 'GET',
    headers:{
        'Content-Type': 'application/json'
    },
    // body:JSON.stringify({
    //     name: 'tata'
    // })
})
.then(res => {
    if(res.ok){
        // debugger;
       return res.json()
        console.log('success');
        window.location.href =  "https://qa.app.kyss.ai/getAnchorDetails?tradeName=tata";
    }
    else{
        console.log('Not success');
    }
})
.then(data => console.log(data))

/*scroll down animation*/

$(function() {
    $('.scroll-down').click (function() {
      $('html, body').animate({scrollTop: $('section.ok').offset().top }, 'slow');
      return false;
    });
  });
/*scroll down animation*/






AOS.init();

// (function() {
//   var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
//   ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
//   var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
// })();
$(function () {
  var fx = function fx() {
  $(".stat-number").each(function (i, el) {
      var data = parseInt(this.dataset.n, 10);
      var props = {
          "from": {
              "count": 0
          },
              "to": { 
              "count": data
          }
      };
      $(props.from).animate(props.to, {
          duration: 1000 * 1,
          step: function (now, fx) {
              $(el).text(Math.ceil(now));
          },
          complete:function() {
              if (el.dataset.sym !== undefined) {
                el.textContent = el.textContent.concat(el.dataset.sym)
              }
          }
      });
  });
  };
  
  var reset = function reset() {
      //console.log($(this).scrollTop())
      if ($(this).scrollTop() > 0) {
          $(this).off("scroll");
        fx()
      }
  };
  
  $(window).on("scroll", reset);
});

// let dhover = document.querySelector('.flexbox-slider');
// let hover = document.querySelector('.flexbox-slide');
// let hoverClass = document.querySelector('.hoverclass');
// let removeClass = () => {
//     // debugger;
//     hover.classList.remove('flexbox-slide-1');
// }
// let addClass = () => {
//     // debugger;
//     hoverClass.classList.add('flexbox-slide-1');
// }
// hover.addEventListener("mouseover", removeClass);
// dhover.addEventListener("mouseout", addClass);

const button = document.querySelector('#submitGstin');
let validateNumber = () => {
    let gstn = document.querySelector('#inputGstin').value;
    // debugger;
    document.querySelector('#inputGstin').classList.remove("error");
    if (gstn.length == 0 || gstn ==""){
// console.log(gstn.length);
document.querySelector('#inputGstin').focus();
document.querySelector('#inputGstin').classList.add("error");
console.log('empty')
return false;
}
else if(gstn.length <= 14){
    console.log(gstn.length);
    return false;
}
else{
    console.log('valid');
    return true;
}
}
button.addEventListener('click', validateNumber);